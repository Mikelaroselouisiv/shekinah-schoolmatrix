# Archive — ne pas utiliser pour le produit actuel

Contenu **obsolète** (ère AWS ECR / prod-locale Windows / Electron assistant / Next dans Docker).

Le produit vivant est :

- `shekinah-schoolmatrix-backend`
- `apps/desktop` (+ `server-stack` pour l’école)
- `apps/sync-agent`
- `infra/` (GCP + ship)

Conservé uniquement pour historique / comparaison. Toute commande sous `_archive/` risque de pointer vers AWS ou d’anciens chemins cassés.
