/** API locale (machine Server / Docker). */
export const LOCAL_API_BASE_URL = 'http://127.0.0.1:3000';

/**
 * URL publique du backend Nest (GCP) — édition Remote.
 * `VITE_API_URL` peut surcharger cette valeur au build.
 */
export const PUBLIC_API_BASE_URL = 'http://34.118.138.96';
